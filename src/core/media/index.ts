/**
 * Media module barrel.
 *
 * PR-0 exports only constants. PR-1 (image-metadata), PR-2 (image-optimize),
 * and PR-3 (attachment-pipeline) will extend this barrel as they land.
 */

export * from './constants.js';
